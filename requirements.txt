# AI-EMS v5.1 — Phase 1~2 의존성
# pip install -r requirements.txt

# === Layer 1: EMS Digital Twin ===
pandapower>=3.0
lightsim2grid>=0.8       # 300+ 버스 고속 솔버
apscheduler>=3.10,<4.0   # SCADA 4초 주기 시뮬

# === API Framework ===
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0,<3.0
httpx>=0.27              # 비동기 HTTP 클라이언트

# === Database ===
redis>=5.0,<6.0
psycopg2-binary>=2.9     # PostgreSQL
# timescaledb는 PostgreSQL 확장 (pip 아님, DB서버에 설치)

# === AI / LLM ===
pydantic-ai>=0.7         # PydanticAI 에이전트 프레임워크
chromadb>=0.4            # 벡터DB (RAG용)
# vLLM / Ollama는 별도 서빙 환경에 설치

# === 로깅 / 설정 ===
structlog>=24.1          # JSON 구조화 로그 (print() 금지)
python-dotenv>=1.0       # .env 환경변수 로드

# === 테스트 ===
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1          # 커버리지 측정 (목표 ≥80%)

# === 시각화 (Phase 2+) ===
streamlit>=1.30
plotly>=5.18
